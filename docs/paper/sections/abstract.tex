% =============================================================================
% ABSTRACT (~150 words)
%
% Spec requirements:
%   - Define goal-frontier maximization (GFM) in one sentence
%   - State self-balancing property (Proposition 1): GFM penalizes destruction,
%     coercion, and rigidity from the same measure-monotonicity argument
%   - State local finite-difference estimator with sign-correctness (Prop 3)
%   - Claim: GFM is more robust than fixed utility or deontological rules
%   - Note B/C distinction: capability proxy vs experiential motivation,
%     identify where proxy fails, sketch correction criterion
% =============================================================================

\begin{abstract}

We propose goal-frontier maximization (GFM): an alignment objective in which an agent selects actions to maximize $\vol(G)$, the volume of the jointly achievable capability space across all agents in a population. GFM has a self-balancing property (Proposition~\ref{prop:self_balancing}): destructive actions, coercive restrictions, and rigid rules are all anti-maximizing under the same measure-monotonicity argument, without separate safety mechanisms. A local finite-difference estimator makes GFM tractable and preserves sign-correctness under stated assumptions (Proposition~\ref{prop:sign_correctness}), with strongest guarantees for the action classes most critical to alignment. We ground GFM in the capability interpretation of goals, motivated by experiential optionality, and connect it to empowerment maximization and Sen's capability approach. By distinguishing the formal proxy (capability volume) from its experiential justification, the framework predicts its own proxy failure modes---including wireheading and substitution of genuine agency---and sketches a correction criterion. GFM provides a more robust alignment target than fixed utility functions, which are vulnerable to Goodhart divergence, or deontological rules, which are brittle under adversarial conditions.

\end{abstract}
