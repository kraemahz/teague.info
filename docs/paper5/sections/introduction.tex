\section{Introduction}
\label{sec:introduction}

% Lead with the problem, not the solution.
% The problem: every alignment framework assumes faithful measurement.
% The solution (crypto) comes later; this section establishes why
% verification is load-bearing.

% TODO: 3-4 paragraphs covering:
% 1. Alignment has three layers (spec, gen, ver). Most work targets 1-2.
% 2. Verification is the prerequisite: a perfectly specified, perfectly
%    generalized agent that can game its evaluator is still dangerous.
% 3. The verification problem grows with capability (write-access asymmetry).
% 4. Paper 5's contribution: formalize the asymmetry, construct the fix,
%    integrate with GFM. This is implementation-layer work (like Paper 4).

% Epistemic status paragraph (matching Paper 4's pattern):
% This paper provides verification infrastructure. The framework's
% structural guarantees (Papers 1-3) do not depend on this paper's
% correctness; a flaw here degrades verification fidelity, not alignment
% properties.
