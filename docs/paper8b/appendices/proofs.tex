\section{Proof Sketches and Technical Notes}
\label{app:proofs}

\subsection{Need-sufficiency architecture
(Section~\ref{sec:need_sufficiency})}

\paragraph{Proposition~\ref{prop:below_suff_saturation}
(Below-Sufficiency Saturation, pointwise).}
Below-sufficiency benchmark for dimension $d_k$ is
$\min(\mathrm{actual}_k / s_k, 1)$, bounded above by $1$
pointwise.  The proposition is deliberately narrow: it
establishes a per-capability, per-dimension cap, not an
aggregate dimension-level cap.  Distinct capabilities each
scoring $1$ on the same dimension accumulate weight under M5;
the broader class of capability-stuffing via distinct-capability
re-instantiation is not blocked by this proposition and is
acknowledged in Open
Question~\ref{oq:capability_stuffing}.

\paragraph{Definition~\ref{def:s1_sufficiency}
(S1--Sufficiency Connection).}
Below-sufficiency sacrifice events fail S1's
non-degrading-alternative condition of the companion
paper~\citep[Revealed~Sacrifice]{lasser2026paper8a} when both
hypotheses of the definition hold: (H1) retention degrades the
agent below sufficiency on some need dimension $d_k$, ruling
out retention as a non-degrading alternative; (H2) no third
non-trade alternative in the agent's choice set is non-degrading.
Together, no
\emph{alternative to the trade itself}---neither retention nor
any third non-trade option---is non-degrading on every need
dimension, which is what S1's
``non-degrading-alternative-to-the-trade'' condition requires;
S1's condition fails and the event is filtered.
The two hypotheses are jointly required for this structural
filter to fire (other S1 failure modes---coercion, duress,
information asymmetry---fail S1 via different routes and
require institutional attestation rather than this filter):
hypothesis~(1) without~(2) allows S1 to hold via a third
alternative; hypothesis~(2) without~(1) is unmotivated since
retention is itself non-degrading.

\paragraph{Proposition~\ref{prop:near_total_collapse}
(Near-Total Collapse Under Unmet Need).}
Under homogeneous sufficiency $s_N(c) \equiv s_N$ for all $c
\in \Down(p_N)$,
$\volL^{\mathrm{eff}} \leq g(s_N) \cdot W_{\mathrm{down}}(N) +
W_{\mathrm{out}}$ (the upper bound holds without
assumptions on other needs because each additional gate factor
is $\leq 1$; full sufficiency of other needs is required only
for the equality case).  Collapse limits as $s_N \to 0$ split:
zero-floor gates ($\alpha_0 = 0$: linear, ReLU, power-law)
contract downstream weight to zero; positive-floor gates
($\alpha_0 > 0$: leaky-ReLU with $\alpha > 0$) contract
downstream weight only to the floor $\alpha_0
W_{\mathrm{down}}$.

\paragraph{Proposition~\ref{prop:need_dominance}
(Need-Dominance in the Gradient).}
Under the common intervention scale (one unit of $\Delta s_N$
and one unit of $\Delta w$ have the same cost), the marginal
return to closing the sufficiency gap on $N$ exceeds the
marginal return to unit-weight addition whenever $g'(s_N) \cdot
W_{\mathrm{down}}(N) > 1$.  The finite-step form via Taylor
expansion (Remark~\ref{rem:dominance_scale}) clarifies that
this is a per-unit-of-intervention comparison, not a claim
about arbitrary-size finite steps.

\subsection{Gap decomposition
(Section~\ref{sec:gap_decomposition})}

\paragraph{Definition~\ref{def:gap_decomp} (Gap Decomposition)
and Proposition~\ref{prop:gap_computable} (Computability).}
Under the default literal-partition strategy~(c), the cells
$\{R, C, D, S, B\}$ form a set-partition of $P$ by stipulation
(boundary-crossing cooperative nodes go to $B$ rather than to a
participant's cell), and $\volL$-additivity follows from the
strategy-(c) atomic-weight accounting convention
$\volL(X) := \sum_{c \in X} w(c)$, which is distinct from the
inherited M1--M6 poset-measure semantics on
poset-independent subsets:
$\volL(P) = \sum_{X \in \{R,C,D,S,B\}} \volL(X)$ since the
cells are set-disjoint and the atomic sum rearranges directly.  The Part-B subset-extension of $\volRlower$ via
per-capability max-attribution
\citep[Revealed~Sacrifice]{lasser2026paper8a} is similarly
set-additive over disjoint cells:
$\volRlower(P) = \sum_X \volRlower(X)$ with $\volRlower(D) =
\volRlower(S) = 0$ by the dormant/residual definitions and
$\volRlower(B)$ a typically-zero contribution from cross-cell
cooperatives whose participants span dormant/residual cells
(non-zero when admissible bundles cover the cooperative
directly; the formula admits either case).  Combining,
$1 - \betaL = \sum_X \delta_X$.  Under cooperative-closure
strategies~(a)/(b), $B = \emptyset$ and the four-cell identity
$\volL(P) = \volL(R) + \volL(C) + \volL(D) + \volL(S)$ requires
M4 over poset-boundary-closed cells (CC1: no cooperative spans
two cells; CC2: no subsumption edge crosses); the $\volRlower$
step is set-additive in either regime and does not require
CC1+CC2.

Computability ($O(|P| + E + M)$) is established by three
passes: (1) per-individual-capability priority classification
in $O(|P|)$; (2) event-sweep updating dormant $\to$ covered in
$O(E)$ with unconditional $\volRlower$ accumulation; (3)
closure-policy pass over the cooperative/subsumption graph in
$O(M \cdot \alpha(M))$ for strategies (a)/(b) via union-find,
or $O(M)$ outright for strategy~(c) via a single hyperedge
sweep classifying boundary-crossing cooperatives into $B$.
The Part~B admissibility label must include the full Part~B
hypothesis set (S0--S5, genuine-exchange conditions,
within-bundle poset-independence from the event-local bundle
decomposition~\citep[Revealed~Sacrifice]{lasser2026paper8a}).

\subsection{Wireheading-consistent concentration signal
(Section~\ref{sec:wireheading})}

\paragraph{Proposition~\ref{prop:wireheading_hhi}
(Concentration Signal).}
$\HHI(\lambda) = \sum_j \lambda_j^2$ is symmetric and
coordinate-convex on the simplex; by the
Hardy--Littlewood--P\'olya
theorem~\citep{hardy1929majorization,marshall2011inequalities},
any such function is Schur-convex: majorization-ordering of
leverage distributions induces monotone HHI ordering.
Cauchy--Schwarz bounds the support-normalized minimum
$\HHI \geq 1/m$; top-$k$-mass concentration bounds the
extremal side via $\HHI \geq \rho^2/k$.  The governance
threshold $\HHI_{\mathrm{alarm}}$ is not theorem-derived
(Remark~\ref{rem:hhi_alarm_governance}).

\paragraph{Proposition~\ref{prop:third_party_wireheading}
(Third-Party Observability of the Part-A variant).}
Let $\mathcal{N}_{S1}$ denote the index set of
S1-admissible committed events as marked by the public
admissibility labelling.  The category-flow statistic
$\tilde\lambda_{\mathrm{trade}}(j,T) =
\sum_{n \in \mathcal{N}_{S1}: \catYn{n} = j}
\Delta\volL(X_n) /
\sum_{n \in \mathcal{N}_{S1}} \Delta\volL(X_n)$ is a direct
function of cleartext-committed fields from the committed
event definition~\citep[Revealed~Sacrifice]{lasser2026paper8a}
together with the public S1-admissibility labelling; no hidden
field (agent identity, bundle-interior subset, $\alpha_{n,c}$
coefficients) enters.  Diagnostic
suppressed when total S1-admissible flow is zero.  Per-capability
variant requires richer commitments or operation within a
consented perimeter~$O$
(Remark~\ref{rem:wireheading_part_b_obs}).

\subsection{Residual class
(Section~\ref{sec:residual_class})}

\paragraph{Proposition~\ref{prop:residual_intersection}
(Residual Class Intersection).}
$\ResB \setminus \ResS$ is witnessed unconditionally by
(b)~artistic practice (identity-individuated, but consumes
material sacrifice).  Whether $\ResS \setminus \ResB$ and
$\ResB \cap \ResS$ are non-empty is conditional on
poset-construction individuation choices.  Conditional
witnesses:
(c)~baseline interoceptive self-awareness (candidate witness
under individuation that distinguishes baseline from trained
acuity; the strict reading would treat clinical measurement
as a sacrifice path attaching to the baseline, in which case
the witness fails);
(d)~private contemplative experience
(identity-individuated, no material footprint, conditional on
treating idle time as not opportunity-cost-attributable).

\paragraph{Proposition~\ref{prop:oi_floor}
(Active-Agent Floor Transfer, conditional on the bridge).}
The transfer from Paper~3's OI
result~\citep[Corollary~2.1]{lasser2026horizon} to this
paper's $\volRexact(O)$ is \emph{conditional} on the
OI--exercise bridge (Definition~\ref{def:oi_exercise_bridge}):
Paper~3 establishes a structural $\volL$ floor on elimination,
not an exercise witness, so the bridge hypothesis supplies
the temporal/semantic conversion to exercise-indicator
semantics.  Where the bridge
holds---identity-coupled participation producing cooperative
output within $[t-\Delta, t]$---the OI floor contributes
$\OIfloor(k) \geq 1$ to $\volRexact^{[W]}(O)$ independently of
the sacrifice channel.  Where it fails (dormant presence),
the structural $\volL$ fact still holds but $\volRexact(O)$
receives no contribution.
